import type {AxiosInstance} from "axios";

export default class Regulatory {
    apiClient: AxiosInstance;
    constructor(apiClient: AxiosInstance) {
        this.apiClient = apiClient;
    }

    // Endpoint fetching functions starts here
}